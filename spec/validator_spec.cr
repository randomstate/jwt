require "./spec_helper"

describe Jwt::Validator do
  it "can validate a jwt token against expected claims" do
  end
  it "can account for clock skew" do
  end
  it "can validate custom header or payload claims using callbacks" do
  end
end
